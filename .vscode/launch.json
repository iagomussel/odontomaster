{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start API",
            "request": "launch",
            "command": "cd api ; npm run dev",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Run Script: start APP",
            "request": "launch",
            "command": "cd app; npm run serve",
            "cwd": "${workspaceFolder}"
        }
    ]
}