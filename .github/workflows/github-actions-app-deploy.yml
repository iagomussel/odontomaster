name: ci

on:
  push:
    branches:
      - 'main'
      - 'develop'
      - 'feature/github_action'
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      -
        name: create env files for app
        run: |
            ls -las
            printf "\nVUE_APP_BUILD=%s\n" ${GITHUB_RUN_ID} >> /github/workspace/app/.env-production
            printf "VUE_APP_BASE_API=/api/\n" >> /github/workspace/app/.env-production
            printf "JWT_KEY=%s\n" "${{ secrets.JWT_SECRET }}" >> /github/workspace/api/.env
            printf "DEFAULT_PASSWORD=123123\n" >> /github/workspace/api/.env
            printf "GITHUB_TOKEN=%s\n" "${{ secrets.TOKEN }}" >> /github/workspace/api/.env

      -
        name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: himmussel/odontomaster:latest